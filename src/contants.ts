export enum PostRoutes {
  BASE = '/posts',
  ALL = '/',
  GETBYID = '/:id',
  CREATE = '/',
  UPDATE = '/:id',
  DELETE = '/:id',
  MULTER_CREATE_FIELD = 'picture',
}

export enum AppConfig {
  API_PATH = '/api',
  MULTER_FOLDER = 'uploads',
}
